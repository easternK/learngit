Git is a distributed version control system.
Git is free software destributed under the GPL.
Git has a mutable index called stages.
Git tracks changes of files.
test git always pass account verify.
I'm sure if I use http to link , git always use account verify.(The command is "git remote add origin https"//github.com/username/repo-name.git")
If I use SSH to link ,we can cancel user authenticcation.(The command is "git remote add origin git@github.com/username/repo-name.git")